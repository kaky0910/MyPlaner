CREATE TABLE check_habit (
	nickname VARCHAR2(30),
	habit VARCHAR2(50),
	month DATE NOT NULL,
	check_habit VARCHAR2(35),
	CONSTRAINT FK_check_habit FOREIGN KEY (nickname,habit) REFERENCES habit(nickname,habit) ON DELETE CASCADE,
	CONSTRAINT PK_check_habit PRIMARY KEY (nickname,habit,month)
);