<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="schedule-mapper">														

	<insert id="addSchedule" parameterType="Schedule">
		<selectKey keyProperty="scheduleNo" resultType="int" order="BEFORE">
			SELECT schedule_seq.nextVal FROM dual
		</selectKey>
		INSERT INTO schedule(
			schedule_no,schedule_title,schedule_start_date,schedule_end_date,schedule_content,schedule_tag,nickname
		) VALUES(
			#{scheduleNo},#{scheduleTitle},#{scheduleStartDate},#{scheduleEndDate},#{scheduleContent},#{scheduleTag},#{nickname}
		)
	</insert>
</mapper>



































